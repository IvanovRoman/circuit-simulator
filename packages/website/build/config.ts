import { resolve } from './utils';

export const assert = resolve('src/assert');

export const output = resolve('dist/');

export const bundleAnalyzer = true;

export const publicPath = '/';
